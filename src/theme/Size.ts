export enum Size {
  XXXS = 1,
  XXS = 2,
  XS = 4,
  SMALL = 8,
  DEFAULT = 16,
  LARGE = 32,
  XL = 48,
  XXL = 64,
  XXXL = 96,
}

export const sizeKeys = Object.keys(Size);
export const sizeVals = Object.values(Size);
